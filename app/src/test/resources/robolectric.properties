# Robolectric has no android-all image for the project's targetSdk (36) yet, and defaults
# to targetSdk when unpinned -- which fails with UnsupportedOperationException from
# DefaultSdkProvider. Pin to the newest SDK Robolectric actually ships.
sdk=35
