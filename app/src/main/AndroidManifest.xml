<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ScribCalApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScribCal">
        
        <!-- Enable bubbles for the app -->
        <property
            android:name="android.permission.ALLOW_BUBBLES"
            android:value="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.ScribCal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Intent filter for receiving shared photos -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <!-- App Actions capabilities; Assistant reads them from the launcher activity. -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

        </activity>

        <!-- Headless target for Assistant capabilities and scribcal:// deep links -->
        <activity
            android:name=".ui.voice.VoiceActionActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:documentLaunchMode="always"
            android:label="@string/app_name"
            android:theme="@style/Theme.ScribCal.Translucent">

            <!-- One action per capability declared in res/xml/shortcuts.xml -->
            <intent-filter>
                <action android:name="com.tjcelaya.scribcal.action.VOICE_START" />
                <action android:name="com.tjcelaya.scribcal.action.VOICE_STOP" />
                <action android:name="com.tjcelaya.scribcal.action.VOICE_RECORD" />
                <action android:name="com.tjcelaya.scribcal.action.VOICE_EXTEND" />
                <action android:name="com.tjcelaya.scribcal.action.VOICE_STATUS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- scribcal://action/{start|stop|record|extend|status}?name=… -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:host="action"
                    android:scheme="scribcal" />
            </intent-filter>
        </activity>

        <!-- Activity for handling notification taps -->
        <activity
            android:name=".ui.notifications.NotificationActionActivity"
            android:exported="false"
            android:theme="@style/Theme.ScribCal.Translucent"
            android:excludeFromRecents="true"
            android:resizeableActivity="true"
            android:documentLaunchMode="always"
            android:allowEmbedded="true" />

        <!-- FileProvider for camera photo sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>